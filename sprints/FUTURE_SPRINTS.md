# Future Sprint Drafts

All planned sprint drafts have now been materialized under `sprints/v1` through
`sprints/v5`.

The remaining incomplete work is not missing planning detail. It is live
evidence that requires external state:

- real Modal-authenticated benchmark runs for the submission bundle
- live verification that the deployed endpoint enforces bearer auth correctly
